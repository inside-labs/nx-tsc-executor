{
    "builders": {
        "watch": {
            "implementation": "./dist/watch",
            "schema": "./dist/watch/schema.json",
            "description": "Runs the dev in watch moe for any typescript node app mode using ts-node and nodemon"
        },
        "dev": {
            "implementation": "./dist/dev",
            "schema": "./dist/dev/schema.json",
            "description": "Runs the dev for any typescript node app mode using ts-node"
        },
        "build": {
            "implementation": "./dist/build",
            "schema": "./dist/build/schema.json",
            "description": "Builds any node typescript project and optionally you can run it after after the build"
        }
    }
}
